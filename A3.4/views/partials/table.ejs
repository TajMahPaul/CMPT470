<table class="table table-condensed">
    <tr>
        <th>ID</th><th>Color</th><th>Width</th><th>Height</th><th>Perimeter</th><th>Area</th>
    </tr>

    <% for (var i = 0; i < rectangles.length; i++) { %>
    <tr>
        <td><%= rectangles[i].id %></td>
        <td><%= rectangles[i].color %></td>
        <td><%= rectangles[i].width %></td>
        <td><%= rectangles[i].height %></td>
        <td><%= rectangles[i].perimeter %></td>
        <td><%= rectangles[i].area %></td>
        <td>
            <button onclick="edit(this)" class="btn btn-primary btn-sm edit-button" >Edit</button>
        </td>
        <td>
            <form action="api/rectangles/delete/<%= rectangles[i].id %>" method="POST">
                <button type="submit" class="btn btn-raised btn-danger btn-sm">Delete</button>
            </form>
        </td>
    </tr>    
    <% } %>
</table>

<!-- Modal -->
<div class="modal" id="edit-modal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">Edit Rectable</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">×</span>
          </button>
        </div>
        <div class="modal-body">
            <form id="edit-form" action="/" method="POST">
                <div class="form-group">
                    <label for="id">Id</label>
                    <input name="id" type="text" class="form-control" id="modal-id" readonly>
                  </div>
                <div class="form-group">
                  <div class="form-group">
                    <label for="color">Color</label>
                    <input name="color" type="text" class="form-control" id="modal-color">
                  </div>
                </div>
                <div class="form-group">
                  <label for="width">Width</label>
                  <input name="width" type="number" class="form-control" id="modal-width">
                </div>
                <div class="form-group">
                  <label for="height">Height</label>
                  <input name="height" type="number" class="form-control" id="modal-height">
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <button type="submit" class="btn btn-primary">Save changes</button>
                </div>
            </form>
        </div>
      </div>
    </div>
  </div>